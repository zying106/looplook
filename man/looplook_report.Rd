% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis.R
\name{looplook_report}
\alias{looplook_report}
\title{Render a Publication-Ready 3D Annotation Report}
\usage{
looplook_report(
  bedpe_file = NULL,
  target_bed = NULL,
  expr_matrix_file = NULL,
  sample_columns = NULL,
  species = "hg38",
  project_name = "looplook Analysis",
  out_dir = "looplook_results",
  threshold = 1,
  unit_type = "TPM",
  reclassify_by_expression = TRUE,
  tss_region = c(-2000, 2000),
  neighbor_hop = 0,
  hub_percentile = 0.95,
  color_palette = "Set2",
  target_mapping_mode = "all",
  include_Filled = TRUE,
  use_nearest_gene = FALSE,
  target_source = "targets",
  loop_types = c("E-P", "P-P"),
  stat_test = "wilcox.test",
  run_go = FALSE,
  run_ppi = FALSE,
  run_motif = FALSE,
  genome_id = species,
  motif_p_thresh = 1e-04,
  motif_ntop = 5,
  motif_n_perm = 0L,
  ppi_score = 400,
  ppi_nSample = 400,
  heatmap_nSample = 99999,
  gsea_nSample = 99999,
  cnet_nSample = 50,
  karyo_bin_size = 1e+05,
  diff_file = NULL,
  lfc_col = "log2FoldChange",
  metadata_file = NULL,
  precomputed_res = NULL,
  chromatin_beds = list(),
  chromatin_bw = NULL,
  bw_ratio_threshold = 3,
  enhancer_bed = NULL,
  output_file = NULL,
  quiet = FALSE,
  seed = NULL,
  universe_genes = NULL,
  ...
)
}
\arguments{
\item{bedpe_file}{Path to a BEDPE file of chromatin loops. Default \code{NULL}
(requires \code{precomputed_res} instead).}

\item{target_bed}{Optional path to a BED file of genomic features.}

\item{expr_matrix_file}{Optional path to a normalised expression matrix.}

\item{sample_columns}{Sample columns in the expression matrix to average.
Default \code{NULL} (all columns).}

\item{species}{Genome assembly (\code{"hg38"}, \code{"hg19"}, \code{"mm10"}, \code{"mm9"}).}

\item{project_name}{Character. Project prefix for the report title.}

\item{out_dir}{Output directory. Created if missing.}

\item{threshold}{Numeric. Expression threshold for active gene classification.
Default \code{1.0}.}

\item{unit_type}{Character. Expression unit label for plot annotations. Default \code{"TPM"}.}

\item{reclassify_by_expression}{Logical. Reclassify silent promoters as eP/eG.
Default \code{TRUE}.}

\item{tss_region}{Numeric vector of length 2. TSS flanking region in bp. Default \code{c(-2000, 2000)}.}

\item{neighbor_hop}{Integer. k-hop ego-network expansion order for loop connectivity analysis. Default \code{0}.}

\item{hub_percentile}{Numeric. Quantile threshold for hub classification. Default \code{0.95}.}

\item{color_palette}{Character. RColorBrewer qualitative palette name. Default \code{"Set2"}.}

\item{target_mapping_mode}{Character. Mapping strategy for target genes. Default \code{"all"}.}

\item{include_Filled}{Logical. Include comprehensively merged gene assignments. Default \code{TRUE}.}

\item{use_nearest_gene}{Logical. Bypass 3D loop-based assignment and use linear proximity. Default \code{FALSE}.}

\item{target_source}{Character vector. Source of target genes to profile. Default \code{"targets"}.}

\item{loop_types}{Character vector. Loop types to include in profiling. Default \code{c("E-P", "P-P")}.}

\item{stat_test}{Character. Statistical test for violin comparisons. Default \code{"wilcox.test"}.}

\item{run_go}{Logical. Run GO enrichment (requires clusterProfiler).
Default \code{FALSE}.}

\item{run_ppi}{Logical. Run protein-protein interaction network analysis. Default \code{FALSE}.}

\item{run_motif}{Logical. Run transcription factor motif analysis. Default \code{FALSE}.}

\item{genome_id}{Character. Reference genome for motif scanning. Defaults to \code{species}.}

\item{motif_p_thresh}{Numeric. P-value threshold for motif enrichment. Default \code{1e-4}.}

\item{motif_ntop}{Numeric. Number of top motifs to display. Default \code{5}.}

\item{motif_n_perm}{Integer. Number of within-component label permutations
for empirical motif P-value estimation. Passed to
\code{\link{profile_target_genes}}. Default \code{0} (Fisher exact test).}

\item{ppi_score}{Numeric. Minimum STRING combined score. Default \code{400}.}

\item{ppi_nSample}{Numeric. Maximum genes to include in PPI. Default \code{400}.}

\item{heatmap_nSample}{Numeric. Maximum genes in expression heatmap. Default \code{99999}.}

\item{gsea_nSample}{Numeric. Maximum genes sampled for GSEA. Default \code{99999}.}

\item{cnet_nSample}{Numeric. Number of GO terms in cnetplot. Default \code{50}.}

\item{karyo_bin_size}{Numeric. Bin size for karyotype heatmaps. Default \code{100000}.}

\item{diff_file}{Optional differential expression result file.}

\item{lfc_col}{Column name for log2 fold change in \code{diff_file}.}

\item{metadata_file}{Optional sample metadata file.}

\item{precomputed_res}{Optional. Either a \code{.RData} file path or an
in-memory list object returned by \code{annotate_peaks_and_loops}.
When provided, annotation is skipped and refinement starts from this object.}

\item{chromatin_beds}{Named list of BED file paths for orthogonal chromatin
mark validation (passed to \code{\link{refine_loop_anchors_by_expression}}).
When non-empty, a \emph{Chromatin Validation} section appears in the report
with confidence-level distribution for eP/eG anchors. Default: \code{list()} (skip).}

\item{chromatin_bw}{Named list of bigWig file paths, or \code{NULL}. Passed
to \code{\link{refine_loop_anchors_by_chromatin}}. Requires \code{"H3K4me1"}
and \code{"H3K4me3"}. \strong{Strongly recommended} for resolving true
dual-signature elements. Default: \code{NULL}.}

\item{bw_ratio_threshold}{Numeric. Minimum H3K4me1/H3K4me3 ratio for dual
classification. Default: \code{3}.}

\item{enhancer_bed}{Character or \code{NULL}. Path to a curated enhancer
BED file (e.g. FANTOM5, ENCODE cCREs). Default: \code{NULL}.}

\item{output_file}{Character. Output HTML file name. \code{NULL} derives from \code{project_name}.}

\item{quiet}{Logical. Suppress rendering output. Default \code{FALSE}.}

\item{seed}{Integer or NULL. Passed to \code{\link{profile_target_genes}} for
reproducible GSEA and motif sampling. Default \code{NULL}.}

\item{universe_genes}{Named numeric vector or \code{NULL}. GO background
universe. Passed to \code{\link{profile_target_genes}}. Default \code{NULL}.}

\item{...}{Additional arguments passed to \code{rmarkdown::render}.}
}
\value{
The path to the generated HTML report (invisibly).
}
\description{
One-click parameterised R Markdown report that executes the full looplook
pipeline (annotation -> refinement -> profiling) and renders an
interpretation-ready HTML document suitable for sharing with collaborators.
}
\details{
The profiling stage uses R's random number generator for GSEA gene-set
down-sampling (\code{gsea_nSample}) and motif background anchor sampling. Call
\code{set.seed()} before \code{looplook_report()} for fully reproducible
results.
}
\examples{
if (requireNamespace("rmarkdown", quietly = TRUE) &&
    requireNamespace("knitr", quietly = TRUE)) {
    temp_env <- new.env()
    load(system.file("extdata", "analysis_results.RData", package = "looplook"), envir = temp_env)
    precomputed_res <- temp_env[[ls(temp_env)[1]]]
    precomputed_res$loop_annotation <- head(precomputed_res$loop_annotation, 6)
    precomputed_res$target_annotation <- head(precomputed_res$target_annotation, 3)
    precomputed_res$promoter_centric_stats <- head(precomputed_res$promoter_centric_stats, 6)
    precomputed_res$distal_element_stats <- head(precomputed_res$distal_element_stats, 6)

    report_path <- looplook_report(
        precomputed_res = precomputed_res,
        project_name = "Example",
        out_dir = tempdir(),
        output_file = "looplook-example-report.html",
        quiet = TRUE,
        run_go = FALSE,
        run_ppi = FALSE,
        run_motif = FALSE
    )
    file.exists(report_path)
}
}
