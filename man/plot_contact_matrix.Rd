% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_processing.R
\name{plot_contact_matrix}
\alias{plot_contact_matrix}
\title{Plot Contact Matrix Heatmap}
\usage{
plot_contact_matrix(
  gi,
  region,
  resolution = 10000,
  palette = c("white", "orange", "red"),
  max_score = NULL,
  ylim_bins = NULL,
  shape = c("diamond", "triangle", "rect")
)
}
\arguments{
\item{gi}{A \code{\link[InteractionSet]{GInteractions}} object.}

\item{region}{Character. Genomic region to plot (e.g., "chr1:100000-200000").}

\item{resolution}{Integer. Bin size in base pairs. Default 10000.}

\item{palette}{Character vector. Color gradient.}

\item{max_score}{Numeric. Cap the score at this value.}

\item{ylim_bins}{Numeric. Limit Y-axis height (zoom in).}

\item{shape}{Character. Shape of the pixels: "diamond" (standard Hi-C), "triangle", or "rect". Default "diamond".}
}
\value{
A ggplot object.
}
\description{
Visualizes chromatin interactions (GInteractions) as a rotated triangular heatmap.
Supports custom shapes (diamond/triangle) with perfect boundary alignment.
}
\examples{
# 1. Load example data
bedpe_path <- system.file("extdata", "example_loops_1.bedpe", package = "looplook")

if (bedpe_path != "") {
  # Convert BEDPE to GInteractions object
  gi <- bedpe_to_gi(bedpe_path)

  # 2. Define a region of interest
  region_str <- "chr1:9893482-13450001"

  # 3. Plot the contact heatmap
  # We use a resolution of 10kb to match the scale of the example loops
  p <- plot_contact_matrix(
    gi = gi,
    region = region_str,
    resolution = 5000,
    shape = "diamond"
  )

  print(p)
}
}
